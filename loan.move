
module flashloan::loan {
    //use sui::sui::SUI;
    //use sui::coin::{Self, Coin};
    //use sui::balance::{Self, Balance};  

    // hot potato struct for loan.
    //public struct Loan {
    //    amount: u64,
    //}

    /// if we create a function that handles with the loan and pushes through all the is 
    /// required for a flash loan to exist

    public fun borrow() {

    }
}